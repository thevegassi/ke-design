The platform / service announcement post — one glyph at 640px inside a 960px circle with a 60px black ring.

```jsx
<IconBadgePost scale={0.3} icon="TablerIconBrandSpotify" />
<IconBadgePost scale={0.3} icon="TablerIconBrandVk" background="var(--ke-oxford-blue)" ring="var(--ke-lime)" />
```

Transcribed from the source file's `Frame 77` / `Property 1=Default` variant set — the file's only Figma component set. Geometry is literal: 60px inset, 960px circle, 60px ring, 640px glyph, 1080px square. Use it for "now on <platform>" announcements; keep the glyph ink black on every lime or light ring.
