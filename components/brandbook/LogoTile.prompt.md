One approved logo-on-ground specimen — the unit the brandbook's colour-combination and alternate-version pages are built from.

```jsx
<LogoTile background="#000000" ink="#FFFFFF" />
<LogoTile background="var(--ke-lime)" ink="#000000" />
<LogoTile image="../../assets/images/concert-crowd.jpg" ink="#FFFFFF" />
<LogoTile outlined ink="#000000" logoHeight={150} height={250} />
```

Photos are always darkened 50% behind the logo. Only the six combinations shown in the brand file are approved — see `Logo.prompt.md`.
