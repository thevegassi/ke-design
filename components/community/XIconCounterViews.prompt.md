View counter — the 16px views glyph variant of the counter.

Transcribed from the source file's `x / icon counter views` symbol — geometry is literal; the palette is re-pointed at Key Entertainment tokens (`--chrome-*`).

```jsx
<XIconCounterViews />
```
