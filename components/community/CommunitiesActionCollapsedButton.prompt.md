Collapsed action menu: 232×183 card, primary button, rule, three menu rows.

Transcribed from the source file's `Communities / action collapsed button` symbol — geometry is literal; the palette is re-pointed at Key Entertainment tokens (`--chrome-*`).

```jsx
<CommunitiesActionCollapsedButton />
```
