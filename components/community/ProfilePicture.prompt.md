Square community profile picture, radius 5, brand-mark fallback.

Transcribed from the source file's `Profile picture` symbol — geometry is literal; the palette is re-pointed at Key Entertainment tokens (`--chrome-*`).

```jsx
<ProfilePicture />
```
