Primary action at the top of the action menu: 199×36 lime button.

Transcribed from the source file's `x/ actions page menu button` symbol — geometry is literal; the palette is re-pointed at Key Entertainment tokens (`--chrome-*`).

```jsx
<XActionsPageMenuButton />
```
