Compact followers card — title, count, one overlapping avatar row.

Transcribed from the source file's `Communities / subscriptions friends small alone` symbol — geometry is literal; the palette is re-pointed at Key Entertainment tokens (`--chrome-*`).

```jsx
<CommunitiesSubscriptionsFriendsSmallAlone />
```
