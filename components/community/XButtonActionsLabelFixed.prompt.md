Secondary action button: hairline border, 13px label, radius 5.

Transcribed from the source file's `x / Button / actions label fixed secondary` symbol — geometry is literal; the palette is re-pointed at Key Entertainment tokens (`--chrome-*`).

```jsx
<XButtonActionsLabelFixed />
```
