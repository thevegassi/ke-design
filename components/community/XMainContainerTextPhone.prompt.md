Phone info row — a phone glyph plus the number in link ink.

Transcribed from the source file's `x / main container text phone` symbol — geometry is literal; the palette is re-pointed at Key Entertainment tokens (`--chrome-*`).

```jsx
<XMainContainerTextPhone />
```
