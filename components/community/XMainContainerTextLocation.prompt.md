Address block: location glyph, address lines, and a map placeholder.

Transcribed from the source file's `x / main container text location` symbol — geometry is literal; the palette is re-pointed at Key Entertainment tokens (`--chrome-*`).

```jsx
<XMainContainerTextLocation />
```
