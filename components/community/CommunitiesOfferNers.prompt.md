Suggestion card in the sidebar — a promoted community with a follow action.

Transcribed from the source file's `Communities / offer ners` symbol — geometry is literal; the palette is re-pointed at Key Entertainment tokens (`--chrome-*`).

```jsx
<CommunitiesOfferNers />
```
