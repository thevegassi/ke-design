The counter strip under a post: likes, comments, shares, then views flush right.

Transcribed from the source file's `x / container counters` symbol — geometry is literal; the palette is re-pointed at Key Entertainment tokens (`--chrome-*`).

```jsx
<XContainerCounters />
```
