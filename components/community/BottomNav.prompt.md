Mobile tab bar: five 24px glyphs, evenly distributed, active tab in lime.

Transcribed from the source file's `bottom nav` symbol — geometry is literal; the palette is re-pointed at Key Entertainment tokens (`--chrome-*`).

```jsx
<BottomNav />
```
