Sidebar info card: 552×422, radius 5, hairline border, header / title / info rows.

Transcribed from the source file's `Communities / container info` symbol — geometry is literal; the palette is re-pointed at Key Entertainment tokens (`--chrome-*`).

```jsx
<CommunitiesContainerInfo />
```
