Tab bar with labels under each glyph — the labelled variant of BottomNav.

Transcribed from the source file's `home bottom menu` symbol — geometry is literal; the palette is re-pointed at Key Entertainment tokens (`--chrome-*`).

```jsx
<HomeBottomMenu />
```
