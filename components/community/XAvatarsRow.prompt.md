A labelled avatar stack: overlapping avatars plus a count.

Transcribed from the source file's `x / avatars row` symbol — geometry is literal; the palette is re-pointed at Key Entertainment tokens (`--chrome-*`) so the recreation carries this brand rather than the platform's.

```jsx
<XAvatarsRow />
```
