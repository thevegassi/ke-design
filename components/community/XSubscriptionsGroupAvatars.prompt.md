Grid of square subscription thumbnails in the community sidebar.

Transcribed from the source file's `x / subscriptions group avatars` symbol — geometry is literal; the palette is re-pointed at Key Entertainment tokens (`--chrome-*`) so the recreation carries this brand rather than the platform's.

```jsx
<XSubscriptionsGroupAvatars />
```
