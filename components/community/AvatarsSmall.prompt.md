Overlapping 24px avatar stack used for follower and friend counts.

Transcribed from the source file's `Avatars / small` symbol — geometry is literal; the palette is re-pointed at Key Entertainment tokens (`--chrome-*`) so the recreation carries this brand rather than the platform's.

```jsx
<AvatarsSmall />
```
