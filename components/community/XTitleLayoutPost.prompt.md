Post header: author name, then a muted timestamp on the same baseline.

Transcribed from the source file's `x / title layout post` symbol — geometry is literal; the palette is re-pointed at Key Entertainment tokens (`--chrome-*`) so the recreation carries this brand rather than the platform's.

```jsx
<XTitleLayoutPost />
```
