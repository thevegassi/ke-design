Subscriptions card: title with a count, then the thumbnail grid.

Transcribed from the source file's `Communities / subscriptions all` symbol — geometry is literal; the palette is re-pointed at Key Entertainment tokens (`--chrome-*`).

```jsx
<CommunitiesSubscriptionsAll />
```
