Section title in the sidebar cards: 12.5px, muted, with an optional count.

Transcribed from the source file's `x / title layout 12,5` symbol — geometry is literal; the palette is re-pointed at Key Entertainment tokens (`--chrome-*`) so the recreation carries this brand rather than the platform's.

```jsx
<XTitleLayout125 />
```
