50px circular avatar; falls back to the brand mark ground when no image is set.

Transcribed from the source file's `x / avatar circle` symbol — geometry is literal; the palette is re-pointed at Key Entertainment tokens (`--chrome-*`) so the recreation carries this brand rather than the platform's.

```jsx
<XAvatarCircle />
```
