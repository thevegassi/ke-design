Engagement counter: 52×24, 24px glyph at 70% opacity, 6px gap, 14px count.

Transcribed from the source file's `x / icon counter` symbol — geometry is literal; the palette is re-pointed at Key Entertainment tokens (`--chrome-*`).

```jsx
<XIconCounter />
```
