Community header block: name, category line and status.

Transcribed from the source file's `x / community title layout` symbol — geometry is literal; the palette is re-pointed at Key Entertainment tokens (`--chrome-*`) so the recreation carries this brand rather than the platform's.

```jsx
<XCommunityTitleLayout />
```
