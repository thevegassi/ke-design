Pill search field in the top bar: 230×28, radius 14, 16px glyph at x=7.

Transcribed from the source file's `x / search header` symbol — geometry is literal; the palette is re-pointed at Key Entertainment tokens (`--chrome-*`) so the recreation carries this brand rather than the platform's.

```jsx
<XSearchHeader />
```
