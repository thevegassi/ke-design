Info row: 24px muted glyph, 8px gap, 13px copy on an 18px line.

Transcribed from the source file's `x / text group info` symbol — geometry is literal; the palette is re-pointed at Key Entertainment tokens (`--chrome-*`).

```jsx
<XTextGroupInfo />
```
