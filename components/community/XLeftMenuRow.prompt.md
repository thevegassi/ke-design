Left-rail navigation row: 24px glyph at 45% opacity, 7px gap, 12.5px label on a 27px line.

Transcribed from the source file's `x / left menu row` symbol — geometry is literal; the palette is re-pointed at Key Entertainment tokens (`--chrome-*`) so the recreation carries this brand rather than the platform's.

```jsx
<XLeftMenuRow />
```
