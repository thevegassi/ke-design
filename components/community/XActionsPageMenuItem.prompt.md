Row inside the community action menu: 200×36, 24px glyph, 13px label.

Transcribed from the source file's `x/ actions page menu item` symbol — geometry is literal; the palette is re-pointed at Key Entertainment tokens (`--chrome-*`).

```jsx
<XActionsPageMenuItem />
```
